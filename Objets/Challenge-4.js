//Modification des Propriétés d'un Objet

const prompt = require("prompt-sync")();

let propriter = {
  x: Number(prompt("Entre votr point X: ")),
  y: Number(prompt("Entre votre point Y: ")),
};
console.log(propriter.x,propriter.y)
